/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.dlt645.readwrite;

import static org.apache.plc4x.java.spi.codegen.fields.FieldReaderFactory.*;
import static org.apache.plc4x.java.spi.codegen.fields.FieldWriterFactory.*;
import static org.apache.plc4x.java.spi.codegen.io.DataReaderFactory.*;
import static org.apache.plc4x.java.spi.codegen.io.DataWriterFactory.*;
import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import java.time.*;
import java.util.*;
import org.apache.plc4x.java.api.exceptions.*;
import org.apache.plc4x.java.api.value.*;
import org.apache.plc4x.java.spi.codegen.*;
import org.apache.plc4x.java.spi.codegen.fields.*;
import org.apache.plc4x.java.spi.codegen.io.*;
import org.apache.plc4x.java.spi.generation.*;

// Code generated by code-generation. DO NOT EDIT.

/**
 * Main frame type per DL/T 645-2007 Section 4.2. Checksum CS = mod-256 sum of all bytes from first
 * 0x68 to before CS (standard-compliant). Data field is stored decoded (-0x33) in memory; checksum
 * uses STATIC_CALL with parsed fields.
 */
public class Dlt645Frame implements Message {

  // Constant values.
  public static final Short START1 = 0x68;
  public static final Short START2 = 0x68;
  public static final Short END = 0x16;

  // Properties.
  protected final byte[] address;
  protected final ControlCode control;
  protected final short length;
  protected final byte[] dataPlain;

  public Dlt645Frame(byte[] address, ControlCode control, short length, byte[] dataPlain) {
    super();
    this.address = address;
    this.control = control;
    this.length = length;
    this.dataPlain = dataPlain;
  }

  public byte[] getAddress() {
    return address;
  }

  public ControlCode getControl() {
    return control;
  }

  public short getLength() {
    return length;
  }

  public byte[] getDataPlain() {
    return dataPlain;
  }

  public short getStart1() {
    return START1;
  }

  public short getStart2() {
    return START2;
  }

  public short getEnd() {
    return END;
  }

  public void serialize(WriteBuffer writeBuffer) throws SerializationException {
    PositionAware positionAware = writeBuffer;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    writeBuffer.pushContext("Dlt645Frame");

    // Const Field (start1)
    writeConstField(
        "start1",
        START1,
        writeUnsignedShort(writeBuffer, 8),
        WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    // Array Field (address)
    writeByteArrayField(
        "address",
        address,
        writeByteArray(writeBuffer, 8),
        WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    // Const Field (start2)
    writeConstField(
        "start2",
        START2,
        writeUnsignedShort(writeBuffer, 8),
        WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    // Simple Field (control)
    writeSimpleEnumField(
        "control",
        "ControlCode",
        control,
        writeEnum(ControlCode::getValue, ControlCode::name, writeUnsignedShort(writeBuffer, 8)),
        WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    // Simple Field (length)
    writeSimpleField(
        "length",
        length,
        writeUnsignedShort(writeBuffer, 8),
        WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    // Manual Array Field (dataPlain)
    writeManualArrayField(
        "dataPlain",
        dataPlain,
        (Byte _value) ->
            org.apache.plc4x.java.dlt645.readwrite.utils.StaticHelper.serializeDataBytePlus33(
                writeBuffer, _value),
        writeBuffer,
        WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    // Checksum Field (checksum) (Calculated)
    writeChecksumField(
        "cs",
        (short)
            (org.apache.plc4x.java.dlt645.readwrite.utils.StaticHelper.calcCs(
                address, control, length, dataPlain)),
        writeUnsignedShort(writeBuffer, 8),
        WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    // Const Field (end)
    writeConstField(
        "end",
        END,
        writeUnsignedShort(writeBuffer, 8),
        WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    writeBuffer.popContext("Dlt645Frame");
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    Dlt645Frame _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();

    // Const Field (start1)
    lengthInBits += 8;

    // Array field
    if (address != null) {
      lengthInBits += 8 * address.length;
    }

    // Const Field (start2)
    lengthInBits += 8;

    // Simple field (control)
    lengthInBits += 8;

    // Simple field (length)
    lengthInBits += 8;

    // Manual Array Field (dataPlain)
    lengthInBits += length * 8;

    // Checksum Field (checksum)
    lengthInBits += 8;

    // Const Field (end)
    lengthInBits += 8;

    return lengthInBits;
  }

  public static Dlt645Frame staticParse(ReadBuffer readBuffer, Boolean response)
      throws ParseException {
    readBuffer.pullContext("Dlt645Frame");
    PositionAware positionAware = readBuffer;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();

    short start1 =
        readConstField(
            "start1",
            readUnsignedShort(readBuffer, 8),
            Dlt645Frame.START1,
            WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    byte[] address =
        readBuffer.readByteArray(
            "address", Math.toIntExact(6), WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    short start2 =
        readConstField(
            "start2",
            readUnsignedShort(readBuffer, 8),
            Dlt645Frame.START2,
            WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    ControlCode control =
        readEnumField(
            "control",
            "ControlCode",
            readEnum(ControlCode::enumForValue, readUnsignedShort(readBuffer, 8)),
            WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    short length =
        readSimpleField(
            "length",
            readUnsignedShort(readBuffer, 8),
            WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    byte[] dataPlain =
        readManualByteArrayField(
            "dataPlain",
            readBuffer,
            (byte[] _values) -> (boolean) ((COUNT(_values)) == (length)),
            () ->
                (byte)
                    (org.apache.plc4x.java.dlt645.readwrite.utils.StaticHelper.parseDataByteMinus33(
                        readBuffer)),
            WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    short cs =
        readChecksumField(
            "cs",
            readUnsignedShort(readBuffer, 8),
            (short)
                (org.apache.plc4x.java.dlt645.readwrite.utils.StaticHelper.calcCs(
                    address, control, length, dataPlain)),
            WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    short end =
        readConstField(
            "end",
            readUnsignedShort(readBuffer, 8),
            Dlt645Frame.END,
            WithOption.WithByteOrder(ByteOrder.BIG_ENDIAN));

    readBuffer.closeContext("Dlt645Frame");
    // Create the instance
    Dlt645Frame _dlt645Frame;
    _dlt645Frame = new Dlt645Frame(address, control, length, dataPlain);
    return _dlt645Frame;
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (!(o instanceof Dlt645Frame)) {
      return false;
    }
    Dlt645Frame that = (Dlt645Frame) o;
    return (getAddress() == that.getAddress())
        && (getControl() == that.getControl())
        && (getLength() == that.getLength())
        && (getDataPlain() == that.getDataPlain())
        && true;
  }

  @Override
  public int hashCode() {
    return Objects.hash(getAddress(), getControl(), getLength(), getDataPlain());
  }

  @Override
  public String toString() {
    WriteBufferBoxBased writeBufferBoxBased = new WriteBufferBoxBased(true, true);
    try {
      writeBufferBoxBased.writeSerializable(this);
    } catch (SerializationException e) {
      throw new RuntimeException(e);
    }
    return "\n" + writeBufferBoxBased.getBox().toString() + "\n";
  }
}
